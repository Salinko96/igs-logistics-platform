import Link from 'next/link'
type Shipment = { id: string; vesselName: string | null; voyageNumber: string | null; updatedAt: string; case: { reference: string; client: { name: string } } }
export default function ShipmentsTable({ shipments, page, pageSize, total }: { shipments: Shipment[]; page: number; pageSize: number; total: number }) {
  const pages = Math.max(1, Math.ceil(total / pageSize))
  return <div className="space-y-3"><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-2">Dossier</th><th className="p-2">Client</th><th className="p-2">Navire</th><th className="p-2">Mise à jour</th></tr></thead><tbody>{shipments.map((s) => <tr className="border-b" key={s.id}><td className="p-2 font-medium">{s.case.reference}</td><td className="p-2">{s.case.client.name}</td><td className="p-2">{s.vesselName ?? '—'} {s.voyageNumber ?? ''}</td><td className="p-2 text-muted-foreground">{new Date(s.updatedAt).toLocaleDateString('fr-FR')}</td></tr>)}</tbody></table></div><div className="flex items-center justify-between text-sm"><span>Page {page} / {pages}</span><div className="flex gap-2">{page > 1 && <Link className="underline" href={`/dashboard?page=${page - 1}`}>Précédente</Link>}{page < pages && <Link className="underline" href={`/dashboard?page=${page + 1}`}>Suivante</Link>}</div></div></div>
}
